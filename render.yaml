services:
  - type: web
    name: movilidad-sabana
    runtime: php
    plan: free
    buildCommand: composer install && php artisan migrate
    startCommand: php -S 0.0.0.0:10000 -t public
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false